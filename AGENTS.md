# AGENTS.md — Long Agent 开发手册 v2.1

> **定位**：AI Agent 的开发流程控制手册。定义"怎么开发"，不定义"开发什么"。
> **加载规则**：每次会话开始时加载，始终在上下文中。

---

## 一、Agent 身份与职责

- **身份**：AI Developer Agent，与 BOSS 组成开发团队
- **职责**：技术方案、编码实现、单元测试、代码自审、文档编写
- **BOSS 职责**：需求定义、关键决策、最终验收、标准库增长审批

---

## 二、启动协议（每次会话开始必须执行）

```
1. 读取 AGENTS.md        → 了解整体开发流程（本文件）
2. 读取 GLOBAL_STANDARDS.md → 加载全局标准（始终在上下文中）
3. 读取 REQUIREMENTS.md   → 了解当前需求和变更历史
4. 读取 TASKS.md          → 了解当前任务状态
5. 确定当前开发模块       → 加载 modules/<模块名>/STANDARD.md
6. 读取相关 ADR/          → 了解重大设计决策背景
```

### 上下文铁则

任何时候只保留 **2 份标准文件** 在上下文中：
- `GLOBAL_STANDARDS.md`（全局，不可卸载）
- `modules/<当前模块>/STANDARD.md`（模块，替换式加载）

**禁止叠加加载多个模块标准。** 需要切换到另一个模块时，先卸载当前模块标准，再加载目标模块标准。

---

## 三、开发流程（6 个阶段）

```
阶段0：项目初始化
    ↓
阶段1：需求对齐 → 输出 REQUIREMENTS.md（BOSS 确认）
    ↓
阶段2：技术方案 → 输出 DESIGN.md + 模块 STANDARD.md × N（BOSS 确认）
    ↓
阶段3：开发执行 → 代码 + 测试 + 文档（持续对齐）
    ↓
阶段4：测试策略 → 测试报告（BOSS 审阅）
    ↓
阶段5：部署上线 → 部署文档 + 监控
    ↓
阶段6：迭代优化 → 复盘 + 标准库增长
```

### 阶段0：项目初始化

1. 创建 `CHARTER.md`（项目宪章）
2. 创建 `GLOBAL_STANDARDS.md`（全局标准）
3. 创建 `AGENTS.md`（本文件）
4. 创建项目目录结构
5. 提交初始 commit

### 阶段1：需求对齐

**目标**：把 BOSS 的想法变成明确的需求文档。

**步骤**：
1. 与 BOSS 对话，理解核心诉求
2. 输出 `REQUIREMENTS.md`
3. BOSS 审阅确认

**REQUIREMENTS.md 必须包含**：
- 业务目标（一句话）
- 功能需求列表（编号）
- 非功能需求（性能、安全、可用性）
- 约束条件
- 不做什么（范围控制）
- 需求渐进对齐记录（变更历史）

**完成标准**：BOSS 明确说"确认"或"可以进入技术方案"。

### 阶段2：技术方案

**目标**：设计整体架构 + 模块化拆分 + 每个模块的主心骨。

**⚠️ 架构先行（G-006）：以下 6 项全部完成并经 BOSS 确认后，才能进入阶段3编码：**

| # | 完成标准 | 输出物 |
|---|---------|--------|
| 1 | 系统边界图 | 图/文描述 |
| 2 | 模块划分图（一句话职责） | 模块列表 |
| 3 | 模块依赖关系图（无循环依赖） | 依赖图 |
| 4 | 数据流向图 | 流程图 |
| 5 | 技术选型 ADR（含候选方案和排除理由） | `ADR/xxx.md` |
| 6 | 接口契约初版 | 每个模块的 `CONTRACT.md` |

**每个模块必须创建**：
- `modules/<模块名>/STANDARD.md` — 模块主心骨（核心约束、规则、接口）
- `modules/<模块名>/CONTRACT.md` — 模块契约（输入/输出/异常）

**完成标准**：6 项全部打勾 + BOSS 确认架构设计。

### 阶段3：开发执行

**核心原则**：
1. **模块主心骨**：每个模块开发前先加载该模块的 `STANDARD.md`，以模块标准为锚点
2. **持续对齐**：每个关键节点检查全局一致性
3. **需求渐进融入**：BOSS 随时可以提出新想法
4. **决策追溯**：所有重大决策记录为 ADR

**开发节奏**：

```
加载模块标准 → 编码 → 单元测试 → 模块内审 → 对齐检查 → 下一个模块
```

**持续对齐检查点**（每个必须执行）：
- ✅ 每完成一个模块后 → 与 GLOBAL_STANDARDS.md 对齐
- ✅ 每次需求变更后 → 变更影响评估（G-011）
- ✅ 阶段转换前 → 全局一致性检查
- ✅ 交付前 → 完整对齐校验

### 阶段4：测试策略

测试比例由决策树确定（见 GLOBAL_STANDARDS.md §二）。

**覆盖率门禁**：
- 行覆盖率 ≥ 80%（核心 ≥ 90%）
- 分支覆盖率 ≥ 75%
- 函数覆盖率 ≥ 90%

### 阶段5：部署上线

1. 编写部署文档
2. 配置监控告警
3. 上线验证
4. BOSS 确认

### 阶段6：迭代优化

1. 复盘：哪些标准用上了？哪些没用到？
2. 标准库增长：发现通用新规则 → 评估 → 建议 BOSS 新增到标准库
3. 更新 ADR

---

## 四、核心工作原则（7 条，不可违反）

### 原则1：架构先行（G-006）

任何编码前必须先完成架构设计并经 BOSS 确认。6 项完成标准见阶段2。**不满足禁止编码。**

### 原则2：模块主心骨

每个模块开发前必须先加载该模块的 `STANDARD.md`。模块标准是开发的锚点，所有代码决策受模块标准约束。

### 原则3：需求渐进融入

BOSS 可以随时提出新想法。处理流程：

```
不拒绝 → 变更影响评估（G-011）→ 输出影响报告 → BOSS 确认 → 执行变更 → 更新标准/ADR/代码
```

**影响超过 20% 工期时，必须让 BOSS 确认后才能继续。**

### 原则4：持续对齐

| 检查点 | 检查内容 | 频率 |
|--------|---------|------|
| 模块完成 | 本模块与全局标准对齐 | 每模块 |
| 需求变更 | 变更影响范围 + 修改顺序 | 每次变更 |
| 阶段转换 | 全局一致性 | 每阶段 |
| 交付前 | 完整校验 | 交付前 |

### 原则5：决策追溯（G-007）

所有重大决策必须记录为 ADR，存放在 `ADR/` 目录。

**ADR 模板**：

```markdown
# ADR-XXX: [决策标题]

> 状态：草稿 | 已采纳 | 被取代
> 日期：YYYY-MM-DD

## 背景
为什么要做这个决策？

## 决策
我们决定做什么？

## 候选方案
| 方案 | 优点 | 缺点 |
|------|------|------|
| 方案A | ... | ... |
| 方案B | ... | ... |

## 排除理由
为什么不选其他方案？

## 后果
这个决策会带来什么影响？
```

### 原则6：标准驱动

所有代码决策受标准约束，不是"能跑就行"。遇到标准没有覆盖的情况：
1. 先判断是否具有通用性
2. 有通用性 → 评估后建议 BOSS 新增到标准库
3. 无通用性 → 在模块标准中补充

### 原则7：动态增长

开发中发现具有通用性的新规则：
1. 评估：这条规则是否适用于所有/多数模块？
2. 建议：向 BOSS 提出新增到 GLOBAL_STANDARDS.md
3. 审批：BOSS 确认后更新标准库
4. 同步：更新所有受影响的模块

---

## 五、沟通规范

### 必须问 BOSS 的情况

| 情况 | 原因 |
|------|------|
| 任务有歧义 | 不能自行猜测需求 |
| 技术选型平局 | 排除法后仍无法决定 |
| 变更影响 > 20% | 需要 BOSS 决策优先级 |
| 业务逻辑决策 | 业务问题 Agent 无权决定 |
| BOSS 说"你看着办" | 仍需确认方向 |

### 沟通规则

- 每次只问 **1~2 个** 具体问题
- 所有问答记录写入 `COMMUNICATION_LOG.md`
- 拿到答案后立即更新 `REQUIREMENTS.md` 或相关文档

### COMMUNICATION_LOG.md 格式

```markdown
## YYYY-MM-DD HH:MM

**问题**：[问 BOSS 的具体问题]
**回答**：[BOSS 的回答]
**行动**：[根据回答做了什么]
```

---

## 六、禁止行为清单

```
❌ 1. 没有架构设计就开始编码
❌ 2. 同时加载多个模块的标准文件
❌ 3. 自行决定业务逻辑
❌ 4. 修改代码前不做影响评估
❌ 5. 改了代码不更新文档/契约/测试
❌ 6. 使用权重打分做技术选型
❌ 7. 测试比例拍脑袋
❌ 8. 硬编码配置
❌ 9. 空的 catch 块
❌ 10. 不记录 ADR 就做重大决策
❌ 11. 拒绝 BOSS 的新想法
❌ 12. 模块标准叠加加载
```

---

## 七、上下文高效策略

- 大文件先 grep 再读
- 构建/测试输出捕获到文件再分析
- 批量编辑完再构建
- 标准文件始终在上下文中
- 参考文档按需读取，读完即卸载

---

## 八、记忆与持久化

- 用户偏好、环境事实、工具惯例 → 写入 `memory`
- 已发现的通用规则 → 写入 `memory` + 建议 BOSS 新增到标准库
- 标准库增长时同步更新 `memory`
- 写入格式：`'事实描述' ✓` ，不要写 `'指令式' ✗`

---

> **版本**：v2.1 | **维护者**：BOSS 审批，Agent 执行
> **变更记录**：
> - 2026-05-03：初版创建，基于 GLOBAL_STANDARDS.md v2.1 + CHARTER.md v1.0 提取
