# ADR-001：Long Agent 技术选型

> **状态**：草稿 | **日期**：2026-05-01 | **决策人**：BOSS

---

## 一、问题定义

Long Agent 是一个**记忆驱动、以结果为导向**的 AI Agent 核心系统。

**核心特征：**
- 三层记忆是核心资产（人格 + 经验 + 标准），记忆丢了 Agent 就变成新的
- 以交付为核心，不是网关/框架/聊天机器人
- Agent 直接面对用户，不需要消息路由层

**MVP 阶段约束：**
- 单人开发（BOSS + AI Agent），没有专职运维
- 零运维：不能有需要独立部署的服务（数据库、消息队列等）
- 快速验证：1-2 周内跑通核心链路（记忆读写 + 意图理解 + 安全）

**V2 阶段目标：**
- Web UI（浏览器交互）
- 多模型路由 + 本地模型降级
- 多用户支持

**质量目标：**
- 可扩展：换 LLM 不改核心代码，加模型不改接口
- 可测试：核心模块覆盖率 > 95%
- 可观测：结构化日志 + 关键指标采集

---

## 二、评估矩阵

所有候选方案使用统一评估维度打分：

| 维度 | 权重 | 说明 |
|------|------|------|
| 功能匹配度 | 25% | 是否满足 MVP + V2 需求 |
| 开发效率 | 25% | 单人开发的速度 |
| 运维成本 | 20% | 是否需要额外运维 |
| 可扩展性 | 15% | V2 迁移成本 |
| 社区生态 | 15% | 遇到问题能否快速解决 |

---

## 三、决策 1：Web 框架

### 3.1 候选方案评分

| 方案 | 功能匹配 | 开发效率 | 运维成本 | 可扩展 | 生态 | 加权分 |
|------|---------|---------|---------|--------|------|--------|
| **FastAPI** | 9 | 8 | 9 | 10 | 8 | **8.85** |
| Flask | 7 | 9 | 9 | 6 | 9 | 7.85 |
| Django | 6 | 6 | 7 | 5 | 9 | 6.45 |

### 3.2 方案对比

| 维度 | FastAPI | Flask | Django |
|------|---------|-------|--------|
| 异步支持 | ✅ 原生 | ❌ 需扩展 | ✅ 原生 |
| 输入校验 | ✅ Pydantic | ❌ 手动 | ✅ Form/Serializer |
| API 文档 | ✅ 自动生成 | ❌ 手动 | ❌ 需配置 |
| 学习曲线 | 中（**对 AI Agent 不是问题**） | 低 | 高 |
| V2 扩展 Web | ✅ 零成本 | ⚠️ 需要重构 | ✅ 自带 |

### 3.3 决策：FastAPI

**核心理由：**
1. MVP 阶段虽然是纯 CLI，但 FastAPI 的路由、依赖注入、Pydantic 模型校验对 V2 加 Web UI 是零成本
2. 自动生成 API 文档（Swagger UI），方便调试
3. 异步支持，LLM 调用是 IO 密集型，异步可以提升并发能力
4. Pydantic = 输入验证，和安全模块天然配合
5. **学习曲线对 AI Agent 不是瓶颈**——AI 本身就能写 FastAPI 代码

**不选其他方案的明确理由：**
- **不选 Flask**：不支持异步原生、没有自动校验、V2 加 Web UI 需要重构
- **不选 Django**：ORM 过度设计（MVP 用 SQLite 不需要 ORM）、Admin 用不上、整体太重（~50MB 依赖 vs FastAPI ~10MB）

### 3.4 回退策略

**如果 FastAPI 不够用：** 实际上 FastAPI 不太可能不够用。如果 V2 需要更高性能，可以迁移到 Starlette（FastAPI 的底层），路由层代码改动 < 5%。

### 3.5 变更优先级

**低**。框架一旦选定，变更成本高。但 FastAPI 足够轻量，风险极低。

---

## 四、决策 2：数据库

### 4.1 候选方案评分

| 方案 | 功能匹配 | 开发效率 | 运维成本 | 可扩展 | 生态 | 加权分 |
|------|---------|---------|---------|--------|------|--------|
| **SQLite** | 9 | 10 | 10 | 5 | 9 | **8.70** |
| Redis | 7 | 7 | 6 | 7 | 8 | 7.05 |
| PostgreSQL | 8 | 6 | 5 | 10 | 9 | 7.40 |

### 4.2 方案对比

| 维度 | SQLite | Redis | PostgreSQL |
|------|--------|-------|-----------|
| 运维 | ✅ 零配置 | ⚠️ 需要 docker run | ❌ 需要安装+配置 |
| 部署 | ✅ 单文件 | ⚠️ 独立进程 | ❌ 独立服务 |
| 备份 | ✅ 复制文件 | ⚠️ RDB/AOF 需要配置 | ❌ 需要 pg_dump |
| 全文搜索 | ✅ FTS5 内置 | ❌ 需要 RediSearch 模块 | ✅ 内置 |
| 过期机制 | ❌ 手动实现 | ✅ TTL 天然支持 | ⚠️ 需要扩展 |
| 数据结构 | ⚠️ 关系型 | ✅ Hash/Sorted Set/List | ⚠️ 关系型 |
| 并发写入 | ⚠️ 单写 | ✅ 高并发 | ✅ 高并发 |
| 数据量 | ⚠️ < 100GB | ⚠️ 受内存限制 | ✅ 无限制 |
| 热区排序 | ⚠️ 需要手动实现 | ✅ Sorted Set O(logN) | ⚠️ 需要索引 |

### 4.3 决策：SQLite + 存储抽象层

**核心理由：**
1. MVP 阶段数据量 < 10 万条记忆，SQLite 完全够用
2. 单人开发，零运维——`memory.db` 单文件，复制即备份
3. Python 内置 `sqlite3` 模块，零依赖
4. SQLite + FTS5 全文搜索，10000 条记录检索延迟 < 50ms
5. 记忆系统的热区排序（access_count + zone）用 SQL ORDER BY 即可实现

**架构决策：存储层抽象**
- MVP 用 SQLite，但通过 `MemoryStorage` 抽象接口隔离存储实现
- V2 加 Redis 后端时，只需新增 `RedisStorage` 实现，业务逻辑零改动
- 参考：OpenClaw 的渠道插件架构（`defineBundledChannelEntry`）、React 的渲染器协议

```
V1:  MemoryManager → MemoryStorage (接口) → SQLiteStorage
V2:  MemoryManager → MemoryStorage (接口) → SQLiteStorage (持久化)
                                                    → RedisStorage (热区缓存)
```

**为什么不选 Redis（详细说明）：**
- Redis 的 TTL 过期机制虽然天然匹配记忆时效性，但 MVP 阶段记忆量小，手动实现过期不增加复杂度
- Redis 的 Sorted Set 热区排序优势在 10 万条以下不明显，SQL ORDER BY 足够
- Redis 需要独立进程（`docker run redis`），违反了 MVP 的"零运维"约束
- Redis 的持久化（RDB + AOF）需要额外配置，否则有丢数据风险
- **Redis 在 V2 作为热区缓存引入**：通过存储抽象层无缝接入

**不选 PostgreSQL：** MVP 阶段单用户不存在并发问题，PostgreSQL 需要独立运维（安装、配置、备份），增加不必要的复杂度。

### 4.4 数据备份策略

**设计原则：事件驱动，不依赖固定时间。**

软件无法保证 24 小时在线。固定时间备份（如"每日凌晨 3:00"）在软件关闭时不会触发，关闭几天后再打开也不会补跑。

**V1 备份触发时机（三个事件）：**

| 触发事件 | 动作 | 说明 |
|---------|------|------|
| 优雅关闭前（SIGTERM/SIGINT） | 立即备份 | 每次退出前必须备份 |
| 对话结束后检查 | 距上次备份 > 24h → 备份 | 轻量检查，不阻塞对话 |
| 启动时检查 | 距上次备份 > 48h → 提醒用户 | 防止长期未备份 |

**备份实现：**
- SQLite 在线备份：`sqlite3.backup()` API，不锁表
- 备份路径：`data/backups/memory_YYYYMMDD_HHMMSS.db`
- 保留最近 3 个备份，自动清理旧备份
- 备份元数据：`data/backups/last_backup.txt`（记录上次备份时间戳）

**V2 增强方案：**
- 增量备份（WAL 归档）
- 远程备份（S3 / 对象存储）
- 备份完整性校验（SHA256）

### 4.5 回退策略

**如果 SQLite 不够用（V2）：**
- 迁移目标：**PostgreSQL**（主存储）+ **Redis**（热区缓存）
- 通过存储抽象层接入，业务逻辑零改动
- 迁移路径：`scripts/migrate_sqlite_to_pg.py`（数据量小时迁移成本低）
- Redis 接入路径：新增 `RedisStorage` 实现，通过配置切换

### 4.6 变更优先级

**高**。数据库迁移成本最高，一旦选定最难回退。但存储抽象层降低了迁移风险。

---

## 五、决策 3：LLM 接入

### 5.1 候选方案评分

| 方案 | 功能匹配 | 开发效率 | 运维成本 | 可扩展 | 生态 | 加权分 |
|------|---------|---------|---------|--------|------|--------|
| **OpenAI API** | 10 | 10 | 9 | 8 | 10 | **9.50** |
| Anthropic Claude | 9 | 9 | 9 | 8 | 8 | 8.70 |
| 本地 Ollama | 6 | 5 | 5 | 5 | 6 | 5.30 |

### 5.2 方案对比（含关键维度）

| 维度 | OpenAI gpt-4o | Anthropic Claude 3.5 | 本地 Ollama |
|------|-------------|---------------------|------------|
| 综合能力 | ✅ 最强 | ✅ 强 | ⚠️ 取决于模型 |
| 上下文窗口 | 128K tokens | **200K tokens** | 128K（Llama 3） |
| 流式响应 | ✅ 原生支持 | ✅ 原生支持 | ✅ 支持 |
| 函数调用/工具调用 | ✅ 最成熟 | ✅ tool use | ⚠️ 取决于模型 |
| SDK 成熟度 | ✅ 5 行代码 | ✅ 成熟 | ⚠️ 需要配置 |
| 费用 | ⚠️ 按 token | ⚠️ 按 token | ✅ 免费 |
| GPU 需求 | ✅ 无 | ✅ 无 | ❌ 需要 GPU |
| 数据隐私 | ⚠️ 数据出本地 | ⚠️ 数据出本地 | ✅ 不出本地 |

### 5.3 决策：OpenAI API (gpt-4o)

**核心理由：**
1. MVP 阶段追求能力，不追求成本优化
2. gpt-4o 在代码生成、意图理解、推理方面综合能力最强
3. 函数调用（function calling）最成熟，V2 工具调用零成本
4. 流式响应原生支持，V1 流式输出到终端零成本
5. Python SDK 成熟，5 行代码搞定
6. 上下文窗口 128K 输入 + 16K 输出，MVP 阶段足够

**架构决策：模型注册表 + LLMProvider 抽象**
- 模型规格（model_name、max_input_tokens、max_output_tokens、encoding）集中在 `model_registry.py` 管理，不硬编码在实现里
- `LLMProvider` 抽象接口隔离具体提供商，V2 换模型不改核心代码
- Token 计算使用 `tiktoken` 官方库精确计算，不使用 chars // 4 估算

```
V1:  AgentLoop → LLMProvider (接口) → OpenAIProvider (gpt-4o)
                                              ↓
                                        model_registry.py (规格从配置读取)
                                        tiktoken (精确 token 计算)
V2:  AgentLoop → LLMProvider (接口) → OpenAIProvider
                                   → AnthropicProvider
                                   → OllamaProvider
```

**为什么不选 Claude（详细说明）：**
- Claude 3.5 的 200K 上下文窗口是优势，但 MVP 阶段 128K 足够
- Claude 的 tool use 能力和 OpenAI 差不多，但 SDK 生态略弱
- **V2 回退链第一个就是 Claude**：如果 OpenAI 不可用，优先切换到 Claude

**为什么不选 Ollama（详细说明）：**
- MVP 阶段没有 GPU，本地模型能力远弱于 gpt-4o
- 部署复杂（需要下载模型、配置 Ollama 服务），违反零运维约束
- **V2 回退链最后一个是本地 Ollama**：作为兜底方案

### 5.4 费用估算

- MVP 阶段预期每天 ~100 次调用
- gpt-4o：输入 $2.50/1M tokens，输出 $10.00/1M tokens
- 平均每次调用 ~2000 tokens（输入 + 输出）
- 月费用估算：100 × 30 × 2000 × $0.000015 ≈ **$9/月**

### 5.5 回退策略

**如果 OpenAI 不可用（V2 实现回退链）：**
1. OpenAI gpt-4o → Anthropic Claude 3.5 → 本地 Ollama（Llama 3）
2. 回退触发条件：连续 3 次调用失败 或 503 错误
3. 回退恢复：主模型恢复后自动切回（冷却 5 分钟后重试）

### 5.6 变更优先级

**低**。换个 SDK 就行，接口标准化（已在 `LLMProvider` 抽象接口中预留）。

---

## 六、决策 4：交互方式

### 6.1 候选方案评分

| 方案 | 功能匹配 | 开发效率 | 运维成本 | 可扩展 | 生态 | 加权分 |
|------|---------|---------|---------|--------|------|--------|
| **REPL（rich）** | 9 | 10 | 10 | 7 | 8 | **8.90** |
| Web UI | 8 | 5 | 6 | 10 | 9 | 7.30 |
| Gateway（WebSocket） | 6 | 4 | 5 | 9 | 7 | 5.90 |

### 6.2 方案对比

| 维度 | REPL（rich） | Web UI | Gateway |
|------|------------|--------|---------|
| 开发速度 | ✅ 1 天 | ❌ 1-2 周 | ❌ 3-5 天 |
| 部署 | ✅ 零配置 | ❌ 需要服务器 | ❌ 需要长连接管理 |
| 用户体验 | ⚠️ 终端 | ✅ 浏览器 | ✅ 实时 |
| 彩色输出/表格/进度条 | ✅ rich 库 | ✅ 前端组件 | ⚠️ 需要协议设计 |
| 扩展性 | ⚠️ V2 需加 Web | ✅ 已为 Web | ✅ 已支持多渠 |

### 6.3 决策：REPL（rich 库）

**核心理由：**
1. MVP 阶段验证核心能力（记忆 + 理解 + 安全），不是验证 UI
2. `rich` 库支持彩色输出、表格、进度条、Markdown 渲染，体验远好于 `input()` + `print()`
3. `prompt-toolkit` 支持历史记录、自动补全，接近 Hermes Agent 的 CLI 体验
4. V2 再加 Web UI（FastAPI 已经准备好了路由）

**具体技术选择：**
- `rich`：终端渲染（彩色、表格、进度条、Markdown）
- `prompt-toolkit`：交互式输入（历史记录、自动补全）
- `typer`：CLI 参数解析（`--config`、`--once` 等）

### 6.4 回退策略

**如果 REPL 不够用（V2）：**
- 迁移目标：FastAPI + 简单 HTML 前端
- 原因：FastAPI 已作为 Web 框架选型，路由层代码改动 < 10%
- REPL 和 Web 可以共存：CLI 用于开发调试，Web UI 用于日常使用

### 6.5 变更优先级

**低**。CLI → Web 是加法，不是替换。两者可以共存。

---

## 七、决策 5：可观测性

### 7.1 候选方案评分

| 方案 | 功能匹配 | 开发效率 | 运维成本 | 生态 | 加权分 |
|------|---------|---------|---------|------|--------|
| **标准 logging + perf_counter** | 9 | 10 | 10 | 9 | **9.60** |
| structlog + Prometheus | 8 | 6 | 5 | 8 | 6.85 |
| OpenTelemetry | 7 | 4 | 4 | 7 | 5.30 |

### 7.2 方案对比

| 维度 | 标准 logging | structlog + Prometheus | OpenTelemetry |
|------|-------------|----------------------|--------------|
| 结构化日志 | ⚠️ 手动 | ✅ 原生 | ✅ 原生 |
| 指标采集 | ⚠️ 手动 | ✅ Prometheus | ✅ OTLP |
| 分布式追踪 | ❌ | ❌ | ✅ |
| 运维成本 | ✅ 零 | ❌ 需要 Prometheus | ❌ 需要 OTel Collector |
| MVP 适用性 | ✅ 足够 | ⚠️ 过度 | ❌ 过度 |

### 7.3 决策：标准 logging + perf_counter

**核心理由：**
1. MVP 阶段零运维——不需要 Prometheus 服务器、不需要 OTel Collector
2. Python 标准库 `logging` + `time.perf_counter()` 足以满足需求
3. 结构化日志用 `extra` 参数实现（`logger.info("event", extra={...})`）
4. V2 再集成 structlog + Prometheus

**具体方案：**
- 日志格式：JSON（通过 `logging.Formatter` 自定义）
- 指标采集：`perf_counter()` 装饰器包裹关键调用
- 日志级别：DEBUG / INFO / WARNING / ERROR

### 7.4 回退策略

**如果标准 logging 不够用（V2）：**
- 迁移目标：structlog + Prometheus
- 迁移成本：低（structlog 兼容 logging 接口）

### 7.5 变更优先级

**低**。可观测性是基础设施，升级不影响业务代码。

---

## 八、决策 6：测试框架

### 8.1 候选方案评分

| 方案 | 功能匹配 | 开发效率 | 生态 | 加权分 |
|------|---------|---------|------|--------|
| **pytest + pytest-asyncio** | 10 | 10 | 10 | **10.00** |
| unittest | 7 | 6 | 8 | 7.00 |
| nose2 | 5 | 5 | 4 | 4.70 |

### 8.2 方案对比

| 维度 | pytest | unittest | nose2 |
|------|--------|---------|-------|
| 异步支持 | ✅ pytest-asyncio | ❌ 需要手动 | ⚠️ 有限 |
| Mock 集成 | ✅ pytest-mock | ⚠️ unittest.mock | ⚠️ 有限 |
| 覆盖率 | ✅ pytest-cov | ⚠️ coverage.py | ⚠️ 有限 |
| Fixture 系统 | ✅ 强大 | ❌ 弱 | ⚠️ 一般 |
| 社区生态 | ✅ 最大 | ✅ 标准库 | ❌ 小众 |

### 8.3 决策：pytest + pytest-asyncio

**核心理由：**
1. Python 测试框架的事实标准
2. `pytest-asyncio` 完美支持异步测试（主循环和 LLM 调用都是异步的）
3. `pytest-mock` 方便 Mock LLM 调用（避免测试时调用真实 API）
4. `pytest-cov` 集成覆盖率统计
5. Fixture 系统强大，可以轻松创建 `temp_memory`、`mock_llm` 等测试夹具

**测试组件清单：**

| 组件 | 用途 |
|------|------|
| pytest | 测试框架 |
| pytest-asyncio | 异步测试支持 |
| pytest-mock | Mock LLM 调用 |
| pytest-cov | 覆盖率统计 |

### 8.4 回退策略

**如果 pytest 不够用：** 实际上 pytest 不可能不够用。如果 V2 需要行为驱动测试，可以加 `pytest-bdd` 插件，零迁移成本。

### 8.5 变更优先级

**低**。测试框架一旦选定，几乎不会变。

---

## 九、决策总结

| # | 选型 | 决策 | 核心理由 | 变更优先级 | V2 迁移目标 |
|---|------|------|---------|-----------|------------|
| 1 | Web 框架 | FastAPI | 异步 + Pydantic + V2 零成本扩展 Web | **低**（难回） | Starlette（如需更高性能） |
| 2 | 数据库 | SQLite | 零运维 + MVP 够用 + 全文搜索 | **高** | PostgreSQL（主存储）+ Redis（热区缓存） |
| 3 | LLM | OpenAI gpt-4o | 能力强 + 工具调用成熟 + 流式支持 | **低**（易变） | Claude 3.5 → Ollama 回退链 |
| 4 | 交互 | REPL（rich） | 快速验证 + rich 体验好 | **低** | FastAPI + HTML 前端（共存） |
| 5 | 可观测性 | logging + perf_counter | 零运维 + MVP 足够 | **低** | structlog + Prometheus |
| 6 | 测试 | pytest + pytest-asyncio | 标准 + 异步支持 + Mock 方便 | **低** | + pytest-bdd（如需 BDD） |

### 变更优先级总览

```
难回 ◄─────────────────────────────────────────► 易变

  SQLite    FastAPI    pytest    logging    REPL    OpenAI
  (数据库)  (框架)     (测试)    (可观测)   (交互)  (LLM)
  高        低         低        低         低      低
```

**含义：**
- **SQLite 最难回退**：数据迁移成本高，选定时要慎重
- **OpenAI / REPL 最易变**：换 SDK 或加前端不影响核心代码
- **FastAPI / pytest**：一旦选定几乎不变，但风险极低

---

## 十、变更记录

| 日期 | 版本 | 变更 | 操作人 |
|------|------|------|--------|
| 2026-05-01 | v1.0 | 初版创建 | 前台 |
| 2026-05-01 | v1.1 | 加评估矩阵、量化评分、明确不选理由、费用估算 | 前台 |
| 2026-05-01 | v1.2 | 按 8 条深度审读意见全面重写：加 LLM 关键维度（上下文窗口/流式/工具调用）、Redis 候选对比、REPL 交互方案、可观测性选型、测试框架选型、回退策略具体化、变更优先级标注、背景描述强化 | 前台 |
| 2026-05-01 | v1.3 | 9 条改进：模型注册表（规格从配置读取不硬编码）、tiktoken 精确 token 计算、context_overflow 恢复流程、后台任务改为对话后触发+每日定时、LLM 测试方案、可观测性 V1 边界声明、数据备份策略、统一 event loop、存储抽象层（MVP SQLite + V2 Redis 无缝接入） | 前台 |
