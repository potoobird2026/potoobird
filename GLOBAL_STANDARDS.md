# GLOBAL_STANDARDS.md — Long Agent 全局标准 v2.1

> **定位**：所有模块必须遵守的底线标准。不可被模块标准覆盖或削弱。
> **加载规则**：始终在上下文中，不可卸载。

---

## 一、代码质量（G系列）

### G-001：配置外部化
- 禁止硬编码 URL、密码、路径、端口、API Key、超时时间
- 所有可配置项通过 `src/config/settings.py`（Pydantic Settings）管理
- `.env` 文件提供默认值，生产环境通过环境变量覆盖

### G-002：统一错误处理
- 错误信息必须包含 **"出了什么" + "怎么解决"**
- 禁止空 `except: pass`（Python）或空 `catch {}`（其他语言）
- 统一错误类型体系：`src/errors/types.py`
- 外部调用必须有保护（try/except + 降级）

### G-003：命名一致性
- 全项目统一命名风格（Python: snake_case 函数/变量, PascalCase 类）
- 同一概念用同一名字（不能 `get` / `fetch` / `query` 混用）
- 模块命名见 `NAMESPACE_MAP.md`

### G-004：防御性编程
- 入口函数必须验证参数（类型、范围、非空）
- 外部调用必须有保护（超时、重试、降级）
- 信任边界：外部输入在边界处验证，内部传递不再重复验证

### G-005：日志规范
- 关键流程必须有日志（进入/退出/异常）
- 日志级别：DEBUG=开发细节, INFO=关键流程, WARNING=可恢复异常, ERROR=不可恢复
- **禁止在日志中输出敏感数据**（密码、Token、个人隐私）
- 日志格式统一：`logger.info(f"[{ctx.loop_id}] 描述: {非敏感变量}")`

### G-006：架构先行
- 任何编码工作开始前，必须先完成架构设计并经 BOSS 确认
- 架构设计 6 项完成标准：
  - [ ] 系统边界图
  - [ ] 模块划分图（一句话职责）
  - [ ] 模块依赖关系图（无循环依赖）
  - [ ] 数据流向图
  - [ ] 技术选型 ADR（含候选方案和排除理由）
  - [ ] 接口契约初版
- **不满足 6 项禁止开始编码**

### G-007：决策追溯
- 所有重大决策必须记录为 ADR，存放在 `ADR/` 目录
- 触发场景：技术选型、架构变更、模块划分变更、重大重构、安全策略变更
- ADR 模板见 AGENTS.md §原则5

### G-008：安全基线
- 输入验证：所有外部输入在边界处验证
- 参数化查询：禁止 SQL 拼接
- 输出编码：展示用户数据时做 HTML 转义

### G-009：可交接性
- 新人/新 Agent 看文档就能接手
- 每个模块必须有 STANDARD.md + CONTRACT.md
- 所有公共函数必须有 docstring（参数、返回值、异常）

### G-010：密钥与 Secret 管理
- Secret 不打印到日志、不编造默认值
- 缺失配置时抛出让用户修复的明确错误
- `.env.example` 提供所有必需配置项的模板

### G-011：变更影响评估
- 任何代码修改前先评估影响范围
- 步骤：量化影响（grep 引用次数）→ 制定修改顺序 → 执行 + 对齐检查
- 修改顺序：被依赖者先改 → 依赖者后改 → 代码 → 引用同步 → 契约 → 文档 → 测试
- 影响超过 2 个模块时，列出完整修改清单

### G-012：记忆系统宽进严出
- 记忆写入不过滤：写入成本极低（SQLite INSERT），不应限制写入
- 记忆淘汰看价值：淘汰成本极高（永久丢失），必须按信息价值淘汰
- 淘汰公式：eviction_score = (N/K)^α，α > 1 时 N 越接近 K 淘汰越激进
- 加载按预算分配：热区40% + 相关30% + 高价值20% + 锚点10%
- 锚点保护：人格层（layer="personality"）、实体定义、is_anchor=True 的记忆不可淘汰
- 所有参数必须有来源标注（真理公式/LLM判断/自适应），禁止魔法数字
- 参考：ADR-008、ADR-009、ADR-010

---

## 二、测试标准

### 覆盖率门禁
| 指标 | 最低要求 | 核心业务 |
|------|---------|---------|
| 行覆盖率 | ≥ 80% | ≥ 90% |
| 分支覆盖率 | ≥ 75% | ≥ 85% |
| 函数覆盖率 | ≥ 90% | ≥ 95% |

### 测试比例决策树
1. 单元基准：库/SDK 75% / CLI 65% / Web/API 60% / 前端 55%
2. 复杂度调整：核心业务 >500行 +10% / 复杂算法 +5% / 简单CRUD -5%
3. 集成比例：外部依赖 ≥3 个 → 20~25% / 1~2 个 → 12~18% / 无 → 8~10%
4. 契约比例：对外 API → 10~15% / 内部调用 → 5~8% / 无 → 0%
5. E2E比例：核心用户流程 ≥3步 → 5~10% / 简单交互 → 3~5% / 无UI → 0%
6. 校验总和 = 100%

---

## 三、项目结构标准

```
项目根目录/
├── AGENTS.md                   ← 开发手册（流程）
├── GLOBAL_STANDARDS.md         ← 本文件（全局标准）
├── PROJECT_STANDARDS.md        ← 项目标准配置（本项目用哪些标准）
├── REQUIREMENTS.md             ← 需求文档（含变更历史）
├── DESIGN.md / DESIGN-V2.md    ← 技术方案文档
├── TASKS.md                    ← 任务列表（含状态）
├── COMMUNICATION_LOG.md        ← 沟通记录
├── CHARTER.md                  ← 项目宪章
├── ADR/                        ← 架构决策记录
│   ├── 001-xxx.md
│   └── 002-xxx.md
└── modules/
    └── <模块名>/
        ├── STANDARD.md         ← 模块标准（主心骨）
        └── CONTRACT.md         ← 模块契约（输入/输出）
```

---

## 四、模块开发规范

### 模块划分决策树
```
Q1: 该职责是否有独立的变更频率？ → 是 → 独立模块
Q2: 该职责是否需要独立测试？     → 是 → 独立模块
Q3: 该职责是否可能被其他模块依赖？ → 是 → 独立模块（通过接口暴露）
Q4: 该职责代码量是否可能超过500行？ → 是 → 独立模块
否则 → 合并到相邻模块

强制规则：
- 被多个模块依赖的代码 → 必须提取为独立模块（shared/core）
- 有外部依赖的代码 → 必须通过接口隔离（_adapter 模式）
- 一个模块最多依赖 3 个其他模块 → 超过则重新划分
- 禁止循环依赖（A→B→A）
```

---

## 五、技术选型规则

使用**排除法 + 优先级排序**，不使用权重打分。

1. **硬性排除**（一票否决）：不满足核心功能 / 不支持平台 / 许可证冲突 / 停止维护 >1年 / 有未修复漏洞
2. **优先级排序**：社区生态 > 功能匹配度 > 长期维护 > 性能
3. **选择**：某方案三个优先级均领先 → 直接选 / 各有优劣 → 选生态更强的 / 仍持平 → 让 BOSS 选

---

## 六、文档标准

### 必须有的文档
| 文档 | 创建时机 | 维护者 |
|------|---------|--------|
| CHARTER.md | 阶段0 | BOSS + Agent |
| GLOBAL_STANDARDS.md | 阶段0 | BOSS 审批 |
| REQUIREMENTS.md | 阶段1 | Agent 维护，BOSS 确认 |
| DESIGN.md | 阶段2 | Agent |
| 模块 STANDARD.md | 阶段2（每个模块） | Agent |
| 模块 CONTRACT.md | 阶段2（每个模块） | Agent |
| ADR/*.md | 重大决策时 | Agent |
| TASKS.md | 持续更新 | Agent |
| COMMUNICATION_LOG.md | 持续更新 | Agent |

### docstring 标准（Python）
```python
async def function_name(param1: str, param2: int = 0) -> ResultType:
    """
    函数的一句话描述。

    详细描述（可选）。

    Args:
        param1: 参数1说明
        param2: 参数2说明，默认值0

    Returns:
        ResultType: 返回值说明

    Raises:
        ValueError: 何时抛出
        LLMError: 何时抛出
    """
```

---

## 七、禁止行为清单

```
❌ 1. 没有架构设计就开始编码
❌ 2. 同时加载多个模块的标准文件
❌ 3. 自行决定业务逻辑
❌ 4. 修改代码前不做影响评估
❌ 5. 改了代码不更新文档/契约/测试
❌ 6. 使用权重打分做技术选型
❌ 7. 测试比例拍脑袋
❌ 8. 硬编码配置
❌ 9. 空的 catch 块
❌ 10. 不记录 ADR 就做重大决策
❌ 11. 拒绝 BOSS 的新想法
❌ 12. 模块标准叠加加载
❌ 13. 模块集成前不做接口契约对照
```

---

> **版本**：v2.1 | **维护者**：BOSS 审批，Agent 执行
> **变更记录**：
> - 2026-05-03：初版创建，基于 AGENTS.md v2.1 + CHARTER.md v1.0 提取
