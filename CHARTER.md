# Long Agent — 项目宪章

> **版本**：v1.0 | **日期**：2026-05-01 | **状态**：草稿

---

## 一、项目目标（一句话）

**开发一个以交付为核心的 AI Agent 系统，以三层记忆为资产，能理解用户意图、执行可验证的任务、并持续自我优化。**

---

## 二、核心原则（不可妥协）

| # | 原则 | 说明 |
|---|------|------|
| 1 | **简单优先，扩展后置** | MVP 只做 3 个模块，跑通 80% 场景 |
| 2 | **硬件稳定性 > 执行效率** | 默认串行，紧急才并行 |
| 3 | **审批是安全卡点** | 危险操作必须暂停等用户确认，不是建议 |
| 4 | **用户掌控一切** | 知识库、模型、权限，用户说了算 |
| 5 | **Agent 有权拒绝** | 超出能力范围的请求，Agent 可以拒绝 |
| 6 | **数据不能丢** | 快照 + 备份 + 持久化，记忆是核心资产 |
| 7 | **接口和实现分离** | 模块通过接口通信，不直接依赖内部实现 |
| 8 | **以结果为导向** | 目标锚定 + 边界控制 + 防止跑偏 |
| 9 | **自动成长** | 三层记忆自动压缩提炼，越用越懂用户 |
| 10 | **冲突预防** | 写入新知识前自动检测冲突 |
| 11 | **任务快照** | 定期保存状态，支持暂停和恢复 |
| 12 | **安全默认** | 默认安全，不是默认不安全 |

> **元规则**：当原则之间发生冲突时，以用户当前需求为准。用户要速度则效率优先，用户要稳定则串行优先。

---

## 三、技术选型

> 详细决策过程见 `ADR-001_技术选型.md`

| 选型 | 决定 | 核心理由 |
|------|------|---------|
| 语言 | **Python 3.12+** | AI 生态最丰富，FastAPI 原生支持 |
| Web 框架 | **FastAPI** | 异步 + Pydantic 校验 + 零成本扩展 Web |
| 数据库 | **SQLite** | 零运维 + MVP 数据量够用 + 单文件部署 |
| LLM 接入 | **OpenAI API (gpt-4o)** | 能力强 + SDK 成熟，V2 再扩展多模型 |
| 前端 | **纯 CLI**（终端交互） | 快速验证核心能力，V2 再加 Web |
| 项目结构 | **按 FastAPI 最佳实践** | 路由、依赖注入、Pydantic 模型从一开始就规范化 |

---

## 四、MVP 范围（V1）

### V1 做这 3 个模块：

| 模块 | 职责 | MVP 目标 |
|------|------|---------|
| **记忆系统** | 三层记忆（人格 HEXACO + 核心 SQLite + 标准 Markdown） | 能存、能读、能检索、能自动压缩 |
| **理解层** | 意图解析 + 追问策略 + 跑偏检查 | 能理解用户意图，不确定时会追问 |
| **安全模块（基础版）** | 冲突检测 + 输入过滤 + 危险操作暂停 | 保护记忆数据质量，危险操作等用户确认 |

### V1 不做的（放到 V2）：

| 模块 | 原因 |
|------|------|
| 执行层 | MVP 阶段不需要复杂任务调度 |
| 交付层 | MVP 不需要三级验证 + 报告生成 |
| LLM 管理 | 一个 OpenAI 模型裸调就够了 |
| 状态机 | 简单 if/else 控制流程即可 |
| 会话管理 | 单会话不需要跨渠道管理 |

---

## 五、不做什么（范围控制）

| 不做 | 原因 |
|------|------|
| 不做多模型路由 | V2 再考虑 |
| 不做 Web UI | CLI 先行 |
| 不做多用户 | 单用户 Agent |
| 不做插件系统 | 复杂度太高，V2 再说 |
| 不做 ACP 协议 | 内部使用，不需要外部智能体接入 |
| 不做多语言 | Python 一种语言搞定 |
| **V1 跨平台文件权限** | Linux/macOS 用 os.chmod(0o600)，Windows 用 icacls 命令。通过 `_set_restricted_permission()` 统一封装 |

---

## 六、风险评估

### 已识别的技术风险

| # | 风险 | 严重程度 | 应对方案 |
|---|------|---------|---------|
| 1 | **缺少 Agent 主循环设计** | 🔴 致命 | 技术方案阶段必须定义主循环（感知→理解→规划→执行→观察→反思→回复） |
| 2 | **模块间调用关系不明** | 🔴 致命 | 技术方案阶段定义模块接口和调用顺序 |
| 3 | **LLM 调用时机不明** | 🟠 严重 | 明确什么情况调 LLM、什么情况用规则 |
| 4 | **工具没有具体定义** | 🟠 严重 | 技术方案阶段定义工具接口规范 |
| 5 | **记忆写入时机模糊** | 🟡 中等 | 明确写入触发条件、写入内容过滤规则 |
| 6 | **状态机与模块割裂** | 🟡 中等 | 简化为 if/else，V2 再做完整状态机 |
| 7 | **缺少错误恢复** | 🟡 中等 | MVP 至少做到：LLM 调用失败重试 3 次、危险操作可中断 |
| 8 | **没有用户交互设计** | 🟡 中等 | CLI 交互模式：输入→处理→输出，支持"停"命令 |

### 已识别的业务风险

| # | 风险 | 严重程度 | 应对方案 |
|---|------|---------|---------|
| 1 | **OpenAI API 不可用** | 🔴 致命 | MVP 阶段单模型 + 重试 3 次；V2 实现模型回退链（OpenAI → 本地模型） |
| 2 | **OpenAI API 费用** | 🟠 严重 | MVP 限制调用次数，记录 token 消耗 |
| 3 | **需求变更** | 🟡 低 | MVP 范围已锁定，变更放到 V2 |

---

## 七、项目结构

```
long-agent/
├── CHARTER.md              # 项目宪章（本文件）
├── README.md               # 开发指南
├── tasks.py                # 一键命令（setup / run / test / lint），跨平台，替代 Makefile
├── .env.example            # 配置模板
├── .gitignore
├── pyproject.toml          # Python 依赖锁定（poetry）
├── docs/                   # 文档
│   ├── adr/                # 架构决策记录
│   │   └── ADR-001_技术选型.md
│   ├── architecture/       # 架构文档
│   └── api/                # API 文档
├── scripts/                # 脚本
│   ├── setup.sh            # 一键安装
│   └── migrate_db.py       # 数据库迁移
├── .github/                # GitHub 配置
│   ├── workflows/          # CI/CD
│   │   └── ci.yml
│   ├── ISSUE_TEMPLATE.md
│   └── PULL_REQUEST_TEMPLATE.md
├── src/
│   ├── __init__.py
│   ├── main.py             # 入口（CLI loop）
│   ├── config/             # 配置管理
│   │   ├── __init__.py
│   │   └── settings.py     # 环境变量 + .env
│   ├── memory/             # 记忆系统
│   │   ├── __init__.py
│   │   ├── models.py       # 数据模型
│   │   ├── manager.py      # MemoryManager
│   │   ├── storage.py      # SQLite 存储层
│   │   └── personality.py  # HEXACO + OU 过程
│   ├── understanding/      # 理解层
│   │   ├── __init__.py
│   │   ├── intent.py       # 意图解析
│   │   ├── clarification.py # 追问策略
│   │   └── validator.py    # 跑偏检查
│   ├── security/           # 安全模块（基础版）
│   │   ├── __init__.py
│   │   ├── conflict.py     # 冲突检测
│   │   ├── filter.py       # 输入过滤
│   │   └── approval.py     # 危险操作暂停
│   ├── llm/                # LLM 调用（MVP 简化版）
│   │   ├── __init__.py
│   │   └── client.py       # OpenAI SDK 封装
│   └── algorithms/         # 科学算法
│       ├── __init__.py
│       ├── confidence_threshold.py
│       ├── clarification_budget.py
│       ├── clarification_strategy.py
│       ├── degradation_controller.py
│       └── logistic_growth.py
├── tests/
│   ├── test_memory/
│   ├── test_understanding/
│   ├── test_security/
│   └── test_algorithms/
└── data/                   # 运行时数据
    ├── memory.db
    ├── personality.md
    └── standards/
```

---

## 八、开发流程

遵循大公司 7 阶段开发流程：

```
立项（本文档）→ 技术方案 → 开发 → Code Review → 测试 → 发布 → 监控
    ①              ②         ③       ④          ⑤      ⑥      ⑦
```

### 阶段① 验收标准（本阶段）

- [x] 项目宪章已撰写
- [x] 技术选型已确定
- [x] MVP 范围已锁定
- [x] 风险已识别并有应对方案
- [ ] BOSS 评审通过

---

## 九、测试策略

| 测试层 | 覆盖率目标 | 覆盖范围 |
|--------|-----------|---------|
| 单元测试 | 核心模块（记忆、安全）> 95%；非核心 > 80% | 所有公共函数 |
| 集成测试 | > 85% | 模块间调用（记忆→理解→安全） |
| E2E | 关键流程 100% | 完整对话流程（输入→处理→输出） |
| 性能 | 关键操作 < 500ms | LLM 调用、记忆检索 |

## 十、成功标准

**V1 成功的标准（可验证）：**

1. **记忆系统**：Agent 能记住用户偏好，下次对话时能正确使用
2. **理解层**：Agent 能正确解析用户意图，不确定时会追问（追问不超过 3 次）
3. **安全模块**：写入记忆前能检测冲突，危险操作会暂停等用户确认
4. **CLI 交互**：用户可以通过终端与 Agent 对话，Agent 能完成简单任务
5. **代码质量**：核心模块测试覆盖率 > 95%，非核心 > 80%，整体 > 85%；所有公共函数有单元测试

---

## 十一、角色分工

| 角色 | 人员 | 职责 |
|------|------|------|
| 产品经理 + 技术负责人 + 最终评审 | BOSS | 需求确认、评审、决策、最终 Approve |
| 开发 + 文档 | 前台（AI Agent） | 写代码、写测试、写文档 |
| 安全审计 | 自动化工具 | bandit + 自定义安全规则 |

> **Code Review 机制**：单人团队无法做到 2 人 Review，由 BOSS 做最终评审。开发过程中所有代码变更提交到 Git，BOSS 通过 PR 进行 Review。

---

## 十二、变更控制

| 变更级别 | 变更类型 | 处理方式 |
|---------|---------|---------|
| **重大** | 技术选型变更、MVP 范围变更 | 需要重新评审 CHARTER + 更新 ADR |
| **中等** | 新增模块、接口变更 | 需要更新 CHARTER + 新增 ADR |
| **轻微** | Bug 修复、代码优化 | 只需在变更记录中记录，不更新 CHARTER |

---

## 十三、版本控制

| 项目 | 内容 |
|------|------|
| 版本号 | v1.0 |
| 审批状态 | 待 BOSS 评审 |
| 下一步 | 阶段②技术方案（DESIGN.md） |
| 有效期 | 到 V1 发布为止 |

---

## 十四、变更记录

| 日期 | 变更 | 操作人 |
|------|------|--------|
| 2026-05-01 | 初版创建 | 前台 |
| 2026-05-01 | 按评审意见全面更新：加 ADR 引用、元规则、LLM 依赖风险、补全目录结构、测试策略分层、角色分工明确、变更控制流程、版本控制信息 | 前台 |
| 2026-05-01 | 审查修复：不做什么列表改为"跨平台文件权限"（移除 Windows 限制）、Makefile→tasks.py | 前台 |
